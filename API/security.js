/** * actions allowed per api */  function actions (request, response) { 	// stubbed data for actions allowed per api	var stub = { 	'api_xxx': ['action1','action2','action3'],					'AC': ['organization.getAll', 'actions.getAllowed'] } ;		// return requested list of api actions	response.contentType = 'application/json';	return JSON.stringify(stub[JSON.parse(request.body)]); }