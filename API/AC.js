/** * Public API variable */var AC = {};/** * internal: use as a module */var module = module || new Object();if (module && module.exports) {	module.exports = AC;}/** * external: request handler comes in here */function controller (request, response) {	debugger;	var 		method,		results;	// TODO: based on request params, call a method generically (no CASE!)	// if multilevel deep, call correctly//	var results = AC[method](request);	results = AC.organization.getAll(); // example};/** * API: public methods */AC.organization = {};AC.organization.getAll = function get() {	return ds.Organization.all();};/** * Private functions, variables and CONSTANTS */function anonymous(xx) {		}/** * Access and Control * (put this in ac modules and require in here) */(function init() {	// get permitted actions (via xhr except for this one place)		var xhr = require(FileSystemSync("EXTENSIONS_USER").path + "Modules/XHR")	var actions = xhr.post("http://127.0.0.1:9201/security/", null, 'AC')  // base url, action url, params	debugger;		// iterate over AC object	// if function not in action list, replace with dummy function that returns error object	// { 'error':401, 'errorMessage':'Unauthorized'}})();function dummyFunction() {	return { 'error':401, 'errorMessage':'Unauthorized'};}