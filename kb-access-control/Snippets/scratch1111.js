//// some variable that is instantiaed by Scripts/required.js



























//kbLogin

//// test changing login param
//var LOGIN = require("./Security/login.js");
//LOGIN;
//LOGIN.authenticate('helo')
//solution.getApplicationByName("ה Access & control (AC)").ds
//LOGIN.login('troy','bobo')
//LOGIN.logout()
//LOGIN.valid()
//logout();
//currentSession()
getUserSessions()

//var dus = solution.getApplicationByName("_AppController (APP)").ds
//var record = dus.Dummy.createEntity();
//record.save();
//dus.Dummy.all();

//solution.getApplicationByName("ה Access & control (AC)")
//var record = ds.User.createEntity()
//record.name = "troy";
//record.password = directory.computeHA1("troy", "bobo");
//record.save()
//ds.User.all()

//ds.User.remove()