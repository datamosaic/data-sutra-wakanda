exports.moveFile = function moveFile(from, to) {	// check to see if upload widget is behaving correctly	if (from[0] != '/') {		from = FileSystemSync('DATA').path + from;	}		var results = KB.callAPI('SA.file.move',[from,to]);		return results};exports.copyFile = function copyFile(from, to) {	// check to see if upload widget is behaving correctly	if (from[0] != '/') {		from = FileSystemSync('DATA').path + from;	}		var results = KB.callAPI('SA.file.copy',[from,to]);		return results};exports.removeFile = function removeFile(file) {	var results = KB.callAPI('SA.file.remove',arguments);		return results};