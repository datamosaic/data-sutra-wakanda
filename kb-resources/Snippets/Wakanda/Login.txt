Login:	- client-side	WAF.onAfterInit = function() {	    WAF.addListener("document", "onLoad", function(event) {  	        var userLoggedIn = WAF.directory.currentUser();	        if(userLoggedIn != null) {	            username = userLoggedIn.fullName; //get the user's full name to display	               //in the component below as long as "username" is declared as a global Variable datasource	            $$("component1").loadComponent("/loggedIn.waComponent");	        } else {	            $$("component1").loadComponent("/login.waComponent");	        }	    });	};		- local storage	btnLogin.click = function btnLogin_click (event)	    {	  if(WAF.directory.login($$('username').getValue(), $$('password').getValue())){	   if($$("cbSave").getValue()){	    localStorage.username = $$('username').getValue();	    localStorage.password = $$('password').getValue();	   }else{	    localStorage.username = "";	    localStorage.password = "";	   }	 	   window.location.href = '/app.waPage/';	  }else{	   $$('message').show();	  };	    };	 		documentEvent.onLoad = function documentEvent_onLoad (event)		    {		  if(typeof localStorage.username !== "undefined"){		   $$('username').setValue(localStorage.username);		   $$('password').setValue(localStorage.password);		   if(localStorage.username !== ""){		    $$("cbSave").setValue(true);		   }		  }		};	- sample project		https://github.com/choisel/Wakanda-Directory/blob/master/Users/Modules/Users.js	- oAuth/JanRain		https://github.com/leo3linbeck/Wakanda-Stuff/blob/master/StrawPoll/SPWidget/Modules/jsOAuth-1.3.6.js	- login by key sample code		http://forum.wakanda.org/showthread.php?6602-loginByKey-error-catching