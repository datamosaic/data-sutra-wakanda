var solfolder = FileSystemSync("SOLUTION").path;var result = SystemWorker.exec("ps -ax");if( result === null ){	console.log( "couldn't execute system command" );}else{	var processes = result.output.toString().split("\n");	var wakprocesses = [];	for( var i = 0; i < processes.length; ++i )	{		if( processes[i].indexOf(solfolder) > -1 || processes[i].indexOf("Wakanda Enterprise Server") > -1 || processes[i].indexOf("wakandaenterprise") > -1 )		{			wakprocesses.push(processes[i]);		}	}	if( wakprocesses.length === 1 )	{		var spaceLoc = wakprocesses[0].indexOf(" ");		if( spaceLoc === 0 )  spaceLoc = wakprocesses[0].indexOf(" ");		var pid = parseInt( wakprocesses[0].substring(0,spaceLoc) );		pid	}}