function doWorkerGameServer (){	var worker = new SharedWorker( "Snippets/workerGameServer.js", "workerGameServer" );	var port = worker.port;  // MessagePort		port.onmessage = function(event)	{		var message = event.data;		switch( message.type )		{			// who would be sending this error? somewhere else in Wak?			case 'error':				debugger;				break;		}	}	wait();}doWorkerGameServer();