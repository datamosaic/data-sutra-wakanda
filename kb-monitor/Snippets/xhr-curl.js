//try {//	var curl = require('curl');//}//catch (e) {//	curl = false;//}//curl == undefined ? false : true//if (curl) {//	'hello'//}//else {//	'nolo'//}// TEST 1 does curl work straight up//curl.curl('-V').console.stdOut.toString();// TEST 2 script the curl wrapperif( 0 ){var httpClient = require("httpClient"),	client,	url; url = "http://www.google.com"; //make the requesturl = encodeURI(url);client = new httpClient.Client();client.open('GET', url);client.setConnectTimeout(5);client.setMaxTime(30);client.send(); //handle the responseif (client.status === 200) {	//work with client.responseText} else {	//deal with errors}client;}// TEST 3 script the xhr wrapperif( 0 ){var xhrtwo = require('XHR');xhrtwo.get('http://www.google.com/',null,null,5)}// TEST 4 script the curl wrapper with POSTif( 1 ){var httpClient = require("httpClient"),	client,	url; url = "http://www.apple.com"; //make the requesturl = encodeURI(url);client = new httpClient.Client();client.open('POST', url);client.setPostParam( { test: "test" } );client.setConnectTimeout(5);client.setMaxTime(30);client.send(); //handle the responseif (client.status === 200) {	//work with client.responseText} else {	//deal with errors}client;}