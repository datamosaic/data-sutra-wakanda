function doSomething(){	var dateNow = new Date();	if( ( dateNow - dateStart ) < duration )	{		console.log( "doSomething(): " + new Date() );	}	else	{		close();	}}onconnect = function(msg){	var port = msg.ports[0];	console.log( "in onconnect()" );	port.postMessage( "ok" );}// anything outside of function definitions is executed in initializationconsole.log( "start of test" );var dateStart = new Date();var duration = 10 * 1000;setInterval( doSomething, 1 * 1000 );  // time in ms//		// this is for messages from worker	// but in this case I don't need messages from it	var port = worker.port;  // MessagePort	port.onmessage = function(event)	{		var message = event.data;		switch( message.type )		{		}	}