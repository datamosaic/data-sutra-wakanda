exports.dateTimeUTCToString = function dateTimeUTCToString( mydate ){	function pad(number)	{		if (number < 10)			return '0' + number;		return number;    }	if( mydate === null )		return "";	if( mydate instanceof Date === false )		return "(not a date)";		var mystamp = mydate.getUTCFullYear() +        '-' + pad(mydate.getUTCMonth() + 1) +  // months in Date objects start at 0!        '-' + pad(mydate.getUTCDate()) +        ' ' + pad(mydate.getUTCHours()) +        ':' + pad(mydate.getUTCMinutes()) +        ':' + pad(mydate.getUTCSeconds()) +        ' UTC';	return mystamp;};exports.timeElapsedToString = function timeElapsedToString( dateBegin, dateEnd ){	var mydur = dateEnd - dateBegin;		var millisecs = mydur % 1000;	mydur = ( mydur - millisecs ) / 1000;  // ms to secs	var secs = mydur % 60;	mydur = ( mydur - secs ) / 60;  // secs to mins	var mins = mydur % 60;	mydur = ( mydur - mins ) / 60;  // min to hours	var hours = mydur % 24;	mydur = ( mydur - hours ) / 24;  // hours to days	var days = mydur;		var mystr = days > 0                                      ? ( days  + "d " ) : "";	mystr +=    days > 0 || hours > 0                         ? ( hours + "h " ) : "";	mystr +=    days > 0 || hours > 0 || mins > 0             ? ( mins  + "m " ) : "";	mystr +=    days > 0 || hours > 0 || mins > 0 || secs > 0 ? ( secs  + "s"  ) : "0";	return mystr;};